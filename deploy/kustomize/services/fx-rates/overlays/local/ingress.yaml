apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fx-rates
spec:
  ingressClassName: traefik
  rules:
    - host: fx-rates.127.0.0.1.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fx-rates
                port:
                  name: http
